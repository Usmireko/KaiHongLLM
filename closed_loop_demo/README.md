# Closed Loop Demo

This line contains the online demo chain: board bundle/action scripts, server
TCP ingest/actions/watcher services, and host orchestration.

Use `scripts/start_demo.sh --dry-run` for local preflight.
