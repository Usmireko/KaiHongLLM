# Closed Loop Demo Server

Server-side Python services and inference orchestration live in `src/`.

Main service launcher:

```sh
bash closed_loop_demo/server/src/server_B/tcp/demo_services.sh status
```
