# Data Train Test Server

Python dataset, validation, derivation, and experiment tooling lives here.

- `src/`: stable entrypoints and importable modules.
- `scripts/tooling/`: experiment and audit utilities.
- `configs/`: schema/contracts and non-secret config material.
